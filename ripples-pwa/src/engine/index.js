// The engine lives in the shared package (packages/engine-kit — published as
// @aaronm84/engine-kit). This shim matches the other engine games so game
// code imports from 'src/engine' everywhere.
export * from '@aaronm84/engine-kit'
